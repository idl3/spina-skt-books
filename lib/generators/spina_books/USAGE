Description:
    Generates the migration file required to create the review model

Example:
    rails generate spina_books:install

    This will create:
        db/migrate/timestamp_create_spina_books_table.rb
